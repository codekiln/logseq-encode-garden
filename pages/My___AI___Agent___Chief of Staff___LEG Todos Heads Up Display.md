see-also:: [[My/AI/Agent/Chief of Staff]], [[My/AI/Agent/Fleet]], [[Logseq/Journal]]

- # LEG Todos Heads Up Display
	- What in [[My/Knowledge/Garden/logseq-encode-garden]] is waiting on [[Person/codekiln]], and what state the repository is in. **Ordering is the priority** — re-order it to name what happens next.
	- **Last swept: [[2026-08-24 Mon]] by Hayward, fourth context window.** If that is not today's date, nothing here has been re-checked since — read the page as a snapshot rather than as status. The sweeping seat moves this line; a sweep that left it alone did not happen.
	- Scope is this garden only. The work role and genai-services have their own Heads Up Displays in the work vault, under `Areas/Chief of Staff/` and `Areas/GenAI Services/GS Director/`; nothing is duplicated here.
	- Items are here because **an answer from you changes what happens**. Anything this seat could settle itself is settled and recorded below rather than asked.
	- ## Waiting on you
		- TODO **Decide whether this repository's history gets scrubbed, and it is one question: are an internal wiki tenant and a private AI-workspace project id sensitive on their own?** Employer context reached this public repo and is now out of every page at `HEAD`, but history still holds it — including `pages/AI___Coding___Technique___Scoping Planning to .ai-coding dir with issue dir.md`, deleted from the graph but live in history, carrying an internal wiki URL with a page id. If the answer is yes, a rewrite plus a GitHub support purge is the only thing that works. **The cost is real and it is why this is yours:** a rewrite changes every commit hash, and this graph cites its own hashes in prose, so it would break its own references; existing clones and forks keep the old objects regardless. If the answer is no, what is already done is enough. Nothing has been rewritten and nothing force-pushed.
		- TODO **Two graph pages still teach a journal practice you deleted.** [[Logseq/Journal]] instructs topic-led narrative blocks, and [[Logseq/Journal/Editorial headings]] exists only to teach writing them wry and allusive. You deleted all three such blocks from the [[2026-08-18 Tue]] journal yourself and have written none since — 08-16, 08-18 and 08-19 are change log only. **This is not theoretical: this seat followed those pages on [[2026-08-24 Mon]] and put five of them in that day's journal**, then removed them. The next agent reading those pages does the same. Recommendation: delete [[Logseq/Journal/Editorial headings]] and cut the narrative instruction from [[Logseq/Journal]]. A convention page is yours to change, which is why it is here rather than done.
			- [[My/AI/Agent/Chief of Staff]] was a third page teaching it, and no longer is — it now carries the settled rule with both halves, that the seat writes only the change log and that prose above it is yours, neither written nor removed. That much was a decision already on record, so it was brought into line rather than asked about. The two pages above state the convention itself, which is the part that is not this seat's to change.
		- TODO **Say what the [[Filed]] / [[Updated]] line form is.** Five variants exist across the graph and no page states which is correct. Recommendation: `- # [[Filed]]` — the plurality, and the only common form satisfying the bullet rule in `logseq-core`. Separately: **234 of 446 journals have a first line with no bullet, and the cause is no longer a guess.** On [[2026-08-24 Mon]] an editor save was seen doing it — today's journal went from `- # [[Filed]]` to `# [[Filed]]` in the working tree during your own editing session, and landed that way in your commit before you put the bullet back. So a tool eats it; it is not a person choosing. Which tool is still open, and the 234 are still untouched.
		- TODO **Whether to create a `Logseq/Entity/Agent` type.** Held since 2026-08-13. Creating the type sets a convention, and 35 `Person/Steve Yegge/Agent/*` pages become its instances the moment it exists.
		- TODO **Which page owns the settings precedence list.** [[Claude/Code/Settings]] and [[Claude/Code/Settings/Override]] both carry it, both are correct, and they will drift apart.
		- TODO **Three aliases, suggested and unwritten**, since aliases are human-curated here: `Google/Workspace/AI` (recommended), a relative-path phrasing for the Google Drive Markdown question, and `Claude/Google Drive` (would skip).
	- ## Repository state
		- **The employer context is out of every page at `HEAD`, and the guard that should have caught it is now running.** Four pages carried it; the worst was not the one that raised the question — `[[GitHub/Action/Runner/ARM/Concept/Overview of Cost and Deployment]]` held seven footnote links into a private AI workspace, and had since 2025-07-22. **`lefthook.yml` has declared an identity guard since 2026-06-10 and it had never once run**, because `lefthook install` was never run in this clone, so `.git/hooks` held nothing but samples. Installed now and passing on real commits. **A whole-repo scan went from 60 findings in 18 files to none.**
			- Most of those 60 were never leaks: home paths in pages whose value is the copied output, and the employer named as a public fact about other people. Installing the guard as it stood would have blocked the seat's own handoff commit over a home path, which is how a guard earns a permanent `--no-verify`. Six such pages are named in `.secretlintignore`; eight lines that state a third party's public affiliation carry an inline exemption with its reason beside it.
		- **The Grok Bot page is filed and pushed**, and the worker's window is closed. [[CursorAI/Explanation/Grok Bot on a Teams seat]] answers the billing question you asked: a Teams seat includes access at no extra charge on Standard as well as Premium, no admin action needed, sourced to Cursor's pricing page and its Grok Bot plans-and-billing doc read [[2026-08-24 Mon]] and corroborated by xAI's note of 2026-08-21. **The name was not a mis-transcription** — Grok Bot is xAI's agent application, real and correctly heard, with its own Cursor docs; it is neither Cursor's Bugbot reviewer nor the Grok models, and the page separates all three because they bill differently. Dov's email is corroborated and was never needed. What can still cost money is usage rather than access: included usage resets weekly and overflow draws on the account's shared on-demand spend, so with on-demand off, running out stops the work instead of billing for it.
		- **[[archive.ph]] is confirmed working on a paid newspaper**, not only on [[Medium]]'s login wall — you opened a paywalled Boston Globe article through it on [[2026-08-24 Mon]] and said it actually worked. Recorded on the page as a dated, attributed test, because everything else there is undated and was written [[2025-10-05 Sun]] and never revisited. It does not settle the institutional-access route, which is a different mechanism: a snapshot service helps only where a snapshot exists for the one URL asked for.
		- **Text was stranded unsent in the Grok Bot worker's input box** — `add it to today's journal`, never processed as a turn. Nothing was lost: the journal is the seat's job today and the page is filed, so the instruction was already satisfied when the window closed. **Asked rather than assumed, and the answer came back:** the peer seat used no `send-keys` today, so a person typed it. That makes it a human-typed instance rather than a third `send-keys` occurrence, which is the open question it would otherwise have spoken to. If it was you, nothing is needed; it is recorded either way.
		- **Clean and pushed as of the sweep.** `main` is the default branch and is checked out; the working tree is empty.
			- Any count written here is stale the moment the seat commits again, this line included. `git fetch origin && git rev-list --left-right --count origin/main...main` is the reading; without the fetch it hides the behind half.
		- **The six commits held since 2026-08-17 are pushed.** They needed a merge first — `origin` had moved thirty commits ahead while the local ref sat still.
		- 18 commits on the day, two of them a peer seat's `docs(tmux):` work in this same checkout. The day's pages are [[My/AI/Agent/Fleet]] and its children, two tmux questions, a git question, and the seat's own log and display.
		- **This checkout is shared.** A peer seat committed into it mid-task today. Any "clean" reading here describes a moment, not a state that holds.
		- **`tmp/` in this repository is tracked and published**, unlike the other gardens the fleet works in. Working notes written there are public.
	- ## Decided here, not asked
		- Pushed the six held commits, merging rather than rebasing, and resolved the journal conflict as a union rather than picking a side.
		- Left the browser profile's account addresses off [[My/AI/Agent/Fleet/Browser]]. The repository is public and the profile directory name is the whole handle an agent needs.
			- That call was right and scoped to the wrong unit, which is why the page still leaked. It withheld the *addresses* and published the domain, and the domain is the employer. Privacy was actively considered at the right moment and the unit chosen was too narrow — no word list would have caught that, because the guard's list did not carry the university name either. The table now names which directory to attach to and which to leave alone, with no account behind any row.
		- Removed today's journal narrative rather than leaving it pending your review, on the strength of your own 2026-08-18 deletion.
		- Did not touch [[Logseq/Journal]] or [[Logseq/Journal/Editorial headings]], because changing a convention page is not this seat's call. That is the first item above.
		- Re-ran the tmux session-name probe rather than publishing the claim already written down, and it needed re-running: the original test used `session:0` targets, the one target shape a dot in the name leaves alone. The finding is now a page with the control and two dot positions on it — [[tmux/Q/Why does a session name containing a dot break every bare -t target?]].
		- Corrected [[My/AI/Agent/Fleet]] where the day's evidence had overtaken it: the session scheme now carries the repository suffix and the no-dot constraint, and the `tmp/` line asks the reader to check per repository instead of asserting an answer that is false in this garden.
		- Left the 24 slash-form date links on pages written today alone. [[Logseq/Date]] calls `[[YYYY/MM/DD]]` acceptable and merely not preferred for new writes, so rewriting committed ones is churn; new writing here uses the journal form.
		- Left the peer seat's `tmux/Option/pane-border-*` pages alone. They are that seat's work in a shared checkout, and reaching into them is what handing over exists to avoid.
		- Redacted at `HEAD` where it cost nothing and declined to where it cost something. The ARM footnotes cited public sources but linked private conversations no reader could open, so each now names its original — no loss. The browser profile table was rewritten rather than removed, because it is load-bearing for an instruction in use.
		- Did not ignore the pages where the employer is a public fact about someone else — three conference pages, a faculty profile, two verbatim public transcripts. Silencing a whole page to quiet a guard is the same mistake as a guard that cries wolf, reached from the other side. Each line carries its exemption and its reason instead, and the rest of every file is still scanned.
		- Verified two inherited claims about the guard rather than acting on them, and both needed it. An in-file exemption written with a colon before its reason is silently ignored, and an exemption on a file's last line is ignored when the file has no trailing newline — which is 491 of 800 pages sampled here. Both produce an exemption that reads as applied, shows correctly in a diff, and does nothing.
		- Kept the Harvard Art Museums article title on [[archive.ph]] while dropping the institutional-access phrasing there and on this page. A public article about an institution is not an affiliation; the guard cannot tell those apart and a person can.
		- Left `72e3b4aa` alone though it swept two of your own edits into a commit under this seat's message and co-author trailer — your LazyVim note and your bullet restoration. Staging named paths protects against other files, not against a second writer in the same file; the missing check was `git diff --cached` before committing, not `git status` before editing. Rewriting a pushed public branch over a trailer costs more than the drift.
