see-also:: [[tmux/Mode]]

- Entered with the `clock-mode` command or `Prefix t`.
- Replaces pane content with a large digital clock.
- Display-only; press any key to exit.
- Configured with `clock-mode-colour` and `clock-mode-style` options.
