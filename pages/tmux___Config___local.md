alias:: [[.tmux.conf.local]], [[tmux.conf.local]]
tags:: [[Local/Override]], [[Local/File]], [[Configuration/File]]

- # `tmux.conf.local`
	- the local overrides for [[tmux/Config]]
	- typically in a file called `.tmux.conf.local` and placed in the [[Home/Dir]].
	- when using [[tmux/oh-my-tmux]], this file is the only supported way to make further edits to the [[tmux/Config]]; one should not edit [[.tmux.conf]] with oh