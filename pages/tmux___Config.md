alias:: [[.tmux.conf]], [[tmux.conf]]

- # `tmux.config`
	- [[Home/Dir]] uses a [[dotfile]]
		- `~/.tmux.config`
	- ==there is no `.` before `tmux.config` when there is a dedicated directory for tmux config such as with `$XDG_CONFIG_HOME` or `~/.config/tmux`==
		- [[XDG/EnvVar/XDG_CONFIG_HOME]]
			- `$XDG_CONFIG_HOME/tmux.conf`
		- [[dot/config]]
			- `~/.config/tmux/tmux.conf`