alias:: [[vim/jumplist]]

- `:help jumplist`
	- [[My Notes]]
		- this is a list of recent jump locations
		- when I open the jump list with `:ju`, I see jumplist position `89` at the bottom jumping to a file I haven't edited in quite some time. I see a scrollbar, and using `CTRL-W` commands I can get down to the jumplist and browse with vim motion commands so as to see the bottom. reading through the help on the jumplist, I can see that 0 is the most recent jump position - it's odd to me that the jumplist doesn't open at the bottom, what use is that? then I have to enter all these commands to see the jumplist. either I'm expected to script it so this file opens at the bottom or script it so the jumplist is displayed in reverse?