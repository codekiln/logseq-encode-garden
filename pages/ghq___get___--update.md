logseq-entity:: [[Logseq/Entity/CLI/Flag]]
tags:: [[ghq/Commands]]

- # ghq get --update
	- Flag of [[ghq/get]]
	- Fetches and updates **all** repositories under `ghq.root` (equivalent to running `git pull` on each clone).
	- Running with no repository argument applies to every local clone managed by [[ghq]].
