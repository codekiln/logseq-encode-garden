- Step 3 of `openspec config profile` configures "custom [[OpenSpec/Workflow]] selection"
	-
	- ```
	   openspec config profile
	  
	  Current profile settings
	    Delivery: both
	    Workflows: 4 selected (core)
	    Delivery = where workflows are installed (skills, commands, or both)
	    Workflows = which actions are available (propose, explore, apply, etc.)
	  
	  ✔ What do you want to configure? Delivery and workflows
	  ✔ Delivery mode (how workflows are installed): Both (skills + commands) [current]
	  ? Select workflows to make available:
	  ❯[x] Propose change
	   [x] Explore ideas
	   [ ] New change
	   [ ] Continue change
	   [x] Apply tasks
	   [ ] Fast-forward
	   [ ] Sync specs
	   [x] Archive change
	   [ ] Bulk archive
	   [ ] Verify change
	   [ ] Onboard
	  
	  ```
		- it's a bit unclear, but from this I can imagine:
		- the single most important thing here is the definition of each of these terms, which is nowhere to be found