logseq-entity:: [[Logseq/Entity/CLI/Flag]]

- # `fzf --preview`
	- Executes a command for each candidate and displays its output in a preview pane
	- `{}` is replaced with the current candidate string; `{+}` for multi-select
