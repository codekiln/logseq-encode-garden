logseq-proxy-url:: logseq://graph/docs?page=Queries

- # Logseq Queries
	- There's a [query builder](logseq://graph/docs?page=Query%20Builder), but I find it difficult to use. Here's an live example:
		- {{query }}
	- [[Logseq/Flashcard]]s can use queries. Just replace the `query` in `{{cards query}}` with the logseq query.
	-