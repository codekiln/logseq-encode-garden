- # Blank flashcards in flashcard review
	- I was working on logseq flashcard review today in in [[Logseq/Flashcard/Review]] with `{{cards ( or [[rg]] [[vim]] [[nvim]] [[fzf]] [[Zoxide]] [[yazi]] [[tmux]] [[Lazygit]] [[Lightroom/Classic]] )}}` and I came across a few flashcards that were blank. I couldn't advance beyond them.
	- DONE **Cause:** empty outline bullets (`-` with no text) nested under otherwise valid `#card` / `[[card]]` blocks. Review walks child blocks; those steps had no renderable body, so the UI showed blank “slides” and felt like a hard stop until each step was graded.
	- DONE **Repo fix:** a deck-scoped scan (same `or` link set as on [[Logseq/Flashcard/Review]], matching ancestor `[[…]]` text plus obvious hub filenames) found **two** cards with stray empty children: [[Lightroom/Classic/Keyshort/Managing Photos and Catalogs/Show in Explorer and Finder]] (two trailing empty bullets) and [[tmux/Keyshort/Pane/Rotate Panes]] (one trailing empty bullet). Those bullets were removed; a repeat scan reports **zero** empty bullets under matching deck cards.
	- DONE **Coverage check:** no matching deck card lacked a substantive answer subtree (ignoring `card-*::` lines). The `[[nvim]]` disjunct did not match any cards by ancestry that the other query links did not already cover (everything in this pass was already tied to another scope link or hub filename).
	- **Docs / tooling:** Logseq docs clone at `~/Documents/GitHub/logseq/docs` — `pages/Flashcards.md`, `pages/Queries.md`. `nbb` was not on `PATH` in the environment used; verification used a small **Python** outline pass (tab depth, `#card` / `[[card]]`, walk deeper bullets until outdent) instead of an [[NBB-Logseq]] script.
