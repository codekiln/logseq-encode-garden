- Settled [[herdr]]'s config down to two lines in `~/.config/herdr/config.toml`: `onboarding = false` so its first-run write stops editing a [[chezmoi]]-managed file, and `[update] version_check = false` so version handling stays with [[mise]]. Its defaults cover the rest.
- [[LazyVim]] was quietly indenting this graph with two spaces instead of the tab [[Logseq]] writes. Added an `.editorconfig` scoped to `pages/` and `journals/` with `indent_style = tab` — it lands after ftplugins, so it beats the LazyVim defaults without touching `~/.config/nvim`.
- asked [[Claude/Code]] to search its conversation history for a keyword. It worked well.
  - CLI uses `~/.claude/sessions/<pid>.json` for live processes, but the name-to-session binding that `--resume` uses is stored *inside* the transcript as appended `custom-title`
- [[Website/Worse on Purpose]] tracks brand-quality decline in physical goods (cookware, running shoes, cars) after private-equity/conglomerate buyouts — same extraction pattern as [[Enshittification]], different mechanism (consolidated manufacturing, not platform lock-in).

- # [[Filed]]

  - [[Claude/Code/Ref/Session Storage]]
  - [[herdr/Q/Can herdr show the current session name, and how do I switch sessions?]]
  - [[LazyVim/Q/Does LazyVim support indenting with tabs the way Logseq expects? If so, how do I indent and de-indent with tabs in markdown?]]
  - [[Website/Worse on Purpose]]

- # [[Updated]]

  - [[Claude/Code/Session/ID]]
  - [[Claude/Code/Q/If I rename a CLI session with /rename, can I resume it in Claude Desktop's Code tab?]]
  - [[herdr]]
  - [[Enshittification]]
